# .github/logo

`%ProjectName%` repository logos go here.

There should be a 'light' and 'dark' version of the logo.

Folder structure looks like this:

```text
.github/logo/
├── light/
│   └── 1020x1024/
│       └── %LogoName%.png
└── dark/
│   └── 1020x1024/
│       └── %LogoName%.png
```
